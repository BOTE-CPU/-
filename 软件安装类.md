# imageglass安装后不能打开图片：

在设置中点击关联格式-取消注册-再点击注册即可成功，如果后续打开imageglass还有存在“幽灵”残留，那就去注册表删除即可，位置在：1.按 Win + R，输入 regedit，回车打开注册表编辑器。2.导航到这个路径： HKEY_CURRENT_USER\Software\Classes\Applications\3.在里面找到一个叫 ImageGlass.exe 的子项4.把指向旧路径的那个右键删除即可。
# antigravity不能登录：
1.开启 TUN 模式或虚拟网卡模式，2.确保代理节点本身能正常访问 Google 服务，且规则（Rule）没有将 googleapis.com 判定为直连（Direct）。可尝试临时切换为全局模式（Global）再测试登录。
注意：建议创建一个代理规则，比如把antigravity的节点固定在日本。
tips:1.第二次打开时若还是登录不进去或者不能回答问题，则代理模式先打开全局，后面再打开规则。
2.如果遇到打开国内软件时不能访问，这是因为系统代理跟tun模式都打开了导致系统冲突的原因，这个时候你应该把系统代理关闭，并且在设置-网络与Internt-代理，找到使用代理服务器，将它设置为关闭
# clash的系统代理跟tun模式区别
1. 权限问题：TUN 必须要有“管理员最高权限”（UAC 弹窗）
系统代理：权限极低。一个普通的小软件，甚至不需要弹任何管理员警告窗，就能悄悄修改浏览器的代理设置。小白用户双击就能用，零门槛。
TUN 模式：必须在 Windows 核心安装一个虚拟网卡驱动（Wintun 驱动）。
安装驱动需要系统最高管理员权限；
如果默认就开 TUN，每次开机或者打开软件，Windows 就会疯狂弹那个暗黄色的大盾牌弹窗（UAC：“你要允许此应用对你的设备进行更改吗？”）；
很多不懂电脑的用户会被弹窗吓到，以为遇到了病毒。
2. 兼容性与“杀毒软件误报”
系统代理：是 Windows 从几十年以前的 Win98 就支持的标准功能，所有杀毒软件、单位内网、校园网都不会管它。
TUN 模式：因为它要在底层拦截所有网卡的流量，机制和某些“黑客抓包工具”非常像。
遇到 360、火绒、各种电脑管家 时，经常会被当成“危险的虚拟网卡注入”直接拦截或弹窗报警；
很多公司的办公内网、VPN（比如上班打卡软件）如果遇到电脑里还有另一个 TUN 网卡，很容易互相排斥导致蓝屏。
软件开发者为了“减少被小白差评”，只能把 TUN 做成一个进阶的高级开关，让真正有需求的人自己去开。
3. 技术发展的历史遗留原因
十几年前大家翻墙：只为了用浏览器看看网页、查查资料，“系统代理”改一下浏览器就足够用了。
现在的需求变了：现在的程序员要用 Git、要跑 Docker、要玩外服游戏、要用 Codex / ChatGPT 的桌面客户端，老旧的系统代理根本管不住这些现代软件，所以近几年 TUN 模式才彻底成为主流标配。
但很多软件的设计惯性依然保留着：“默认用最不容易出事的系统代理，进阶用户请手动去点 TUN 模式”。
