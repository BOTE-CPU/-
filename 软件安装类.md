# imageglass安装后不能打开图片：

在设置中点击关联格式-取消注册-再点击注册即可成功，如果后续打开imageglass还有存在“幽灵”残留，那就去注册表删除即可，位置在：1.按 Win + R，输入 regedit，回车打开注册表编辑器。2.导航到这个路径： HKEY_CURRENT_USER\Software\Classes\Applications\3.在里面找到一个叫 ImageGlass.exe 的子项4.把指向旧路径的那个右键删除即可。
# antigravity不能登录：
1.开启 TUN 模式或虚拟网卡模式，2.确保代理节点本身能正常访问 Google 服务，且规则（Rule）没有将 googleapis.com 判定为直连（Direct）。可尝试临时切换为全局模式（Global）再测试登录。
注意：建议创建一个代理规则，比如把antigravity的节点固定在日本。
tips:1.第二次打开时若还是登录不进去或者不能回答问题，则代理模式先打开全局，后面再打开规则。
2.如果遇到打开国内软件时不能访问，则把系统代理跟tun模式都关闭，再打开国内网站，之后再重新打开代理应该就可以了
